# 《凡人修仙》网页版 · 从零架设完整教程

> 全程免费、不需要买服务器、不需要懂编程。跟着做，大约 **30 分钟**就能上线给朋友玩。
> 每一步都有 ✅ **检查点**，看到检查点描述的样子再往下走，就不会出错。

---

## 目录

- [第 0 步：准备账号与文件](#第-0-步准备账号与文件)（3 分钟）
- [第 1 步：创建仓库](#第-1-步创建仓库)（2 分钟）
- [第 2 步：创建专用钥匙 Token](#第-2-步创建专用钥匙-token)（3 分钟 · 最关键）
- [第 3 步：上传代码](#第-3-步上传代码)（5 分钟）
- [第 4 步：填写配置](#第-4-步填写配置)（3 分钟）
- [第 5 步：启动自动汇总](#第-5-步启动自动汇总)（3 分钟）
- [第 6 步：开启网页托管，拿到网址](#第-6-步开启网页托管拿到网址)（3 分钟）
- [第 7 步：试玩验证](#第-7-步试玩验证)（5 分钟 · 必做）
- [第 8 步：解决 GitHub 连不上](#第-8-步解决-github-连不上)（10 分钟 · 强烈建议）
- [第 9 步：手机访问与分享](#第-9-步手机访问与分享)
- [第 10 步：后台管理怎么用](#第-10-步后台管理怎么用)
- [常见问题排查表](#常见问题排查表)
- [上线检查清单](#上线检查清单)

---

## 第 0 步：准备账号与文件

1. 打开 https://github.com 注册一个账号（已有就登录）。
   - 用户名建议用英文，例如 `zhangsan2026`。**记下这个用户名**，后面要用很多次。
2. 把发给你的 `xiuxian-game.zip` 解压到电脑桌面，得到一个 `xiuxian` 文件夹。

> 全程**记下三样东西**，建议先复制到记事本：
> - 你的 GitHub 用户名：`____________`
> - 仓库名（固定填）：`xiuxian-game`
> - Token（第 2 步生成）：`____________`

---

## 第 1 步：创建仓库

1. 登录后打开 https://github.com/new
2. 按下面填：

   | 项目 | 填什么 |
   |---|---|
   | Repository name | `xiuxian-game` |
   | 公开/私有 | 选 **Public**（公开，否则免费托管打不开） |
   | Add a README file | ☑ 勾上 |

3. 点绿色按钮 **Create repository**

✅ **检查点**：页面跳转到 `github.com/你的用户名/xiuxian-game`，能看到一个 README.md 文件列表，说明仓库建好了。

---

## 第 2 步：创建专用钥匙 Token

> Token 就是一把「钥匙」，让网页游戏有权限读写你这个仓库里的存档文件。
> 我们只给**这一个仓库**的权限，即使泄露也影响不到你其他项目。

1. 点右上角**头像** → **Settings**
2. 左侧最下面 → **Developer settings**
3. **Personal access tokens** → **Fine-grained tokens**
4. 点 **Generate new token**

按下面填：

| 项目 | 填什么 |
|---|---|
| Token name | `xiuxian`（随便起） |
| Expiration | 选 **90 days** 或 **No expiration**（建议 90 天，到期重新生成一个） |
| Resource owner | 选你自己 |
| Repository access | 选 ☉ **Only select repositories** → 点 **Select repositories** → 只勾 `xiuxian-game` |

5. 往下找到 **Permissions**，点开后展开 **Repository permissions**：

   | 权限名 | 选什么 |
   |---|---|
   | **Contents** | **Read and write**（读写文件内容 —— 存档全靠它） |
   | **Actions** | **Read and write**（后台手动触发汇总要用） |
   | **Metadata** | Read-only（默认就有） |

6. 点最下面绿色 **Generate token**

7. **立刻复制**那串以 `github_pat_` 开头的字符串，粘贴到记事本保存。

> ⚠️ 这串字符**只显示这一次**，刷新页面就没了。丢了就得重新生成一次。

✅ **检查点**：记事本里有一串 `github_pat_xxxxxxxxxxxx` 开头的字符串。

---

## 第 3 步：上传代码

推荐用**最简单的网页上传**（不用装任何软件）。

1. 回到仓库首页 `github.com/你的用户名/xiuxian-game`
2. 点中间的蓝色链接 **uploading an existing file**（或点 **Add file** → **Upload files**）
3. 把桌面 `xiuxian` 文件夹**里面所有的文件和文件夹**拖进去：

   ```
   index.html
   setup.html
   README.md
   教程.md
   css/            （整个文件夹）
   js/             （整个文件夹）
   admin/          （整个文件夹）
   data/           （整个文件夹）
   scripts/        （整个文件夹）
   worker/         （整个文件夹）
   .github/        （整个文件夹，注意开头有个点）
   ```

   > 💡 如果拖文件夹没反应或文件漏了，就分几次拖：先拖所有文件，再逐个拖文件夹。
   > 
   > 💡 `.github` 是隐藏文件夹（开头带点）。如果看不到，Windows 资源管理器 → 查看 → 勾选「隐藏的项目」。或者改用下面的命令行方式。
   >
   > 💡 上传后如果只有文件没有文件夹（扁平成一堆文件），说明浏览器不支持拖文件夹，**必须改用命令行方式**，否则游戏跑不起来。

4. 页面下方 Commit changes 区域，直接点绿色 **Commit changes**

### 命令行方式（如果上面拖拽失败）

```bash
cd 桌面/xiuxian
git init
git add .
git commit -m "init xiuxian game"
git branch -M main
git remote add origin https://github.com/你的用户名/xiuxian-game.git
git push -u origin main
```

弹出登录框：**用户名**填你的 GitHub 用户名，**密码**填第 2 步的 Token（不是登录密码！）。

✅ **检查点**：回到仓库首页，文件列表里能看到 `index.html`、`js`、`css`、`admin`、`data`、`.github` 这些条目，**层级是对的**（点进 `js` 能看到 4 个 .js 文件）。

---

## 第 4 步：填写配置

> 这一步是告诉游戏「你的仓库在哪、钥匙是什么」。

### 方式 A：用配置向导（推荐，最省事）

1. 在电脑上**双击**解压出来的 `setup.html`，会用浏览器打开一个配置页面
   （页面所有内容只在你自己的浏览器里处理，不会上传到任何地方）
2. 页面里填：
   - GitHub 用户名
   - 仓库名（默认 `xiuxian-game`）
   - Token（第 2 步那串）
   - 加速地址（第 8 步做完再回来填，先留空）
3. 点 **生成配置**，再点 **📋 复制配置**
4. 回到仓库，打开 `js` → `api.js`，点右上角铅笔图标 ✏️ 编辑
5. 把文件开头那段 `const GH = { ... };`（大约前 25 行）**整段删除**，粘贴刚复制的内容
6. 点 **Commit changes**

> 💡 页面上还有「🔌 测试连接」按钮，能立刻验证用户名和 Token 对不对。
> 如果它报「连不上」，多半是你家网络访问不了 GitHub，**不代表填错了** —— 先继续往下做，第 8 步会彻底解决。
>
> 💡 页面上的「⬇️ 下载完整 api.js」按钮在**本地双击打开时不能用**（浏览器安全限制），
> 要等第 6 步开完网页托管后，在线访问 `你的网址/setup.html` 才能用。本地请用「复制配置」。

### 方式 B：直接在网页上改（也很快）

1. 仓库里打开 `js` 文件夹 → 点 `api.js`
2. 点右上角铅笔 ✏️ 进入编辑
3. 最上面找到这段：

```js
const GH = {
  owner: 'YOUR_GITHUB_NAME',
  repo: 'xiuxian-game',
  branch: 'main',
  dataBranch: 'main',
  token: 'YOUR_FINE_GRAINED_TOKEN',
  extraEndpoints: [],
};
```

改成（注意保留引号）：

```js
const GH = {
  owner: '你的用户名',
  repo: 'xiuxian-game',
  branch: 'main',
  dataBranch: 'main',
  token: 'github_pat_你的Token',
  extraEndpoints: [],
};
```

4. 点绿色 **Commit changes**

✅ **检查点**：`js/api.js` 里看不到 `YOUR_GITHUB_NAME` 和 `YOUR_FINE_GRAINED_TOKEN` 这两个词了。

---

## 第 5 步：启动自动汇总

> 排行榜、在线人数、每日备份都靠 GitHub 的定时任务（Actions）自动跑，它相当于你的**免费服务器**。

### 5.1 打开写权限

1. 仓库页面点 **Settings**（右上角那一排标签里）
2. 左侧 **Actions** → **General**
3. 拉到最下面 **Workflow permissions**
4. 选 ☉ **Read and write permissions**
5. 点 **Save**

### 5.2 手动跑一次，确认能用

1. 点仓库顶部 **Actions** 标签
2. 左侧列表点 **定时汇总排行榜与统计**
3. 右边点 **Run workflow** → 再点绿色 **Run workflow**
4. 等 30 秒左右，刷新页面

✅ **检查点**：出现一条运行记录，前面是 **绿色 ✔**。点进去能看到日志里有「发现 N 个玩家存档」（刚开始是 0 个，正常）。

> 如果是红色 ✘，点进去看错误，99% 是第 5.1 步权限没开，或者 Token 没勾 Actions 权限。

---

## 第 6 步：开启网页托管，拿到网址

1. 仓库 **Settings** → 左侧 **Pages**
2. **Source** 选 **Deploy from a branch**
3. **Branch** 选 `main`，右边文件夹选 `/ (root)`
4. 点 **Save**
5. 等 **1～3 分钟**，刷新页面，顶部会出现绿色提示：

   ```
   Your site is live at https://你的用户名.github.io/xiuxian-game/
   ```

✅ **检查点**：浏览器打开这个网址，能看到游戏登录界面（黑底星空 + 「凡人修仙」四个大字）。

**你的两个网址**（记下来）：

| 用途 | 网址 |
|---|---|
| 🎮 游戏 | `https://你的用户名.github.io/xiuxian-game/` |
| ⚙️ 后台 | `https://你的用户名.github.io/xiuxian-game/admin/` |

---

## 第 7 步：试玩验证（必做）

### 7.1 创建角色

1. 打开游戏网址
2. 道号填 `测试道人`，口令填 `123456`
3. 点 **登录 / 创建**

✅ **检查点 1**：进入游戏主界面，能看到打坐的小人、灵气粒子在往上飘、底部状态栏显示「☀ 云端已连接」（绿点）。

如果底部显示「离线模式」（红点），直接跳到第 8 步。

### 7.2 确认存档真的写到 GitHub 了

1. 打开仓库 → 进 `data` 文件夹 → `players` 文件夹
2. 应该能看到一个新文件，名字像 `u1a2b3c.json`

✅ **检查点 2**：点开这个文件，里面是你的角色数据（道号、灵石、境界）。**看到它就说明整条数据链路打通了。**

### 7.3 试一次战斗

1. 游戏里点底部「⚔️ 历练」标签
2. 选「后山竹林」，点 **开始历练**

✅ **检查点 3**：能看到两个角色对打、攻击时前冲、受伤时闪白、伤害数字飘起来、血条变化、下方战报滚动。打完提示获得修为和灵石。

### 7.4 试一次后台

1. 打开后台网址 `.../admin/`
2. 点「👥 玩家管理」→ 点 **刷新列表**

✅ **检查点 4**：能看到「测试道人」这一行，可以点「编辑」改他的灵石。改完去游戏里刷新页面，数值变了。

---

## 第 8 步：解决 GitHub 连不上

> 国内访问 `api.github.com` 经常超时，这是**最需要处理的问题**。
> 游戏已经内置了两层兜底（自动换备用通道 + 断网离线玩不丢档），但**自建一个免费代理**才能真正稳定。

### 8.1 先看内置的兜底有没有生效

游戏里点「⚙️ 设置」→ 看「当前通道」。
- 显示 `api.github.com` 且绿点 = 你家网络正常连得上，可以跳过这一步
- 显示别的一串域名 = 已经自动切到备用通道了
- 红点离线 = 需要往下做

### 8.2 自建免费加速代理（Cloudflare Worker，10 分钟）

**A. 注册 Cloudflare**
1. 打开 https://dash.cloudflare.com/sign-up ，用邮箱注册（免费）
2. 一路跳过引导即可，不需要买域名、不需要接入网站

**B. 装上部署工具**
1. 打开 https://nodejs.org 下载安装 **Node.js**（一路下一步）
2. 装完打开「命令提示符 / 终端」，执行：

```bash
npm install -g wrangler
```

**C. 部署代理**

```bash
cd 桌面/xiuxian/worker
npx wrangler login          # 会弹出浏览器让你授权，点 Allow
npx wrangler deploy         # 部署
```

部署完终端会显示一行地址，形如：

```
https://xiuxian-gh-proxy.你的账号.workers.dev
```

**复制这个地址。**

**D. 把钥匙藏进代理（重要，这样前端就不暴露 Token）**

```bash
npx wrangler secret put GH_TOKEN
```

粘贴第 2 步的 Token，回车。

**E. 把地址填进游戏（游戏和后台共用同一份配置，改一次两处都生效）**

*立刻生效（只对你这台设备）*：游戏里「⚙️ 设置」→「自定义加速地址」框里粘贴刚才那个地址 → 点 **保存加速地址** → 自动重新测速。

*对所有人生效*：编辑仓库里的 `js/api.js`，把 `extraEndpoints` 改成：

```js
extraEndpoints: ['https://xiuxian-gh-proxy.你的账号.workers.dev'],
```

点 **Commit changes**。这样别人打开游戏也走加速通道。

> 💡 嫌麻烦就跳过改仓库这步，只用「设置页填一次」也行；但每个玩家都得自己填一次。

✅ **检查点**：游戏设置页「当前通道」显示你自己的 workers.dev 地址，底部绿点。

> 💡 如果配了 `GH_TOKEN` secret，可以把 `js/api.js` 里的 `token` 改成空字符串 `''`，钥匙由代理自动注入，前端页面里就彻底看不到 Token 了。

---

## 第 9 步：手机访问与分享

- 手机浏览器直接打开游戏网址就能玩，界面是手机优先设计的
- **iPhone**：Safari 打开 → 分享 → 「添加到主屏幕」，就变成一个 App 图标
- **安卓**：Chrome 打开 → 右上角菜单 → 「添加到主屏幕」
- 把网址发给朋友，他们注册道号就是**同一个服**的玩家，能看到同一个天骄榜

**换设备继续玩**：记住自己的道号 + 口令，在新手机登录即可，存档在云端。

---

## 第 10 步：后台管理怎么用

打开 `.../xiuxian-game/admin/`：

### 📊 数据看板
总道友数、10 分钟在线、今日新增、灵石总量、境界分布柱状图、最近活跃玩家。

### 👥 玩家管理
1. 搜索框填道号 → 点 **搜索**
2. 点该行 **编辑**
3. 直接改灵石 / 修为 / 境界 / 击杀，或者「封禁」下拉选「封禁」
4. 点 **保存修改**

> **发补偿的正确姿势**：比如想给玩家 500 灵石，看他现在是 1200，就在灵石栏填 `1700`，然后点 **发放补偿**（不是点保存修改）。

> **删档**：编辑状态下点「删除存档」，会二次确认。

### 📢 公告活动
- 公告框写内容 → 点 **发布**
- 勾选「双倍修为」「双倍灵石」就是全服活动，玩家端 **2 分钟内自动生效**，不用重新部署

### 🎛 数值配置
直接改 `game.json` 的 JSON 文本 → **保存配置**。常见改法：
- 怪太强/太弱：改 `maps` 里 `monsters` 的 `hp` / `atk`
- 升级太慢：改 `cultivate` 的 `basePerSec`（修炼速度）
- 想加礼包码：在 `giftCodes` 里加一行 `"CODE123": { "stone": 888, "desc": "节日礼包" }`

### 🧰 工具
手动触发一次汇总、清除浏览器缓存。

---

## 常见问题排查表

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 登录一直转圈 / 提示连接失败 | Token 填错或没勾 Contents 读写 | 重新生成 Token，确认勾了 Read and write |
| 提示「口令有误」 | 这个道号被别人占了，或你换了口令 | 换个道号 |
| 底部一直红点「离线模式」 | api.github.com 连不上 | 做第 8 步自建代理 |
| 能玩但去仓库看不到存档文件 | 存档进了离线队列 | 游戏设置页点「立即同步」；等网络恢复会自动补传 |
| Actions 一直是红色 ✘ | 权限没开 | Settings → Actions → General → Read and write permissions → Save |
| 网址打不开 404 | Pages 还没部署完 | 等 3 分钟再刷新；确认 Pages 的 Branch 选的是 `main` |
| 页面能开但一片空白 | 文件层级传错了 | 检查仓库根目录有没有 `index.html`，`js` 文件夹里有没有 4 个 .js |
| 改了数值游戏里没变 | 浏览器缓存 | 玩家端强制刷新（Ctrl+F5）；数值是每次进游戏拉取的，一般刷新即可 |
| 榜单一直是空的 | Actions 没跑过 | 第 5.2 步手动 Run 一次；且要等有玩家存档后才有数据 |
| Actions 突然不跑了 | GitHub 会停用 60 天无活动的定时任务 | 去 Actions 手动 Run 一次就复活了 |
| 玩一段时间后仓库提交记录特别多 | 每人 30 秒存一次档 | 正常。人多可把 `js/main.js` 里 `setInterval(() => save(), 30000)` 的 30000 改成 60000 |
| 网页改代码后要等一会才生效 | Pages 重新部署要 1～2 分钟 | 正常。但改 `data/config/*.json` **不受影响**，立刻生效 |

---

## 进阶：玩家变多后要做的两件事

### 1. 把数据挪到独立分支（避免 Pages 反复重建）

玩家每次存档 = 一次提交，而 Pages 部署的是 main 分支，会被反复触发重新部署。

1. 仓库首页，点分支下拉框（显示 `main` 那个）→ 输入 `players` → 点 **Create branch players**
2. 编辑 `js/api.js`，把 `dataBranch: 'main'` 改成 `dataBranch: 'players'`
3. 仓库 **Settings** → 左侧 **Secrets and variables** → **Actions** → **Variables** → **New repository variable**
   - Name 填 `DATA_BRANCH`，Value 填 `players`
4. 提交保存

之后所有玩家存档都写在 `players` 分支，不再触发 Pages 重建。**网址和玩法完全不变。**

### 2. 想改游戏内容

所有数值、地图、怪物、掉落、礼包码都在 `data/config/game.json`，后台「🎛 数值配置」页直接改，**不用碰代码**。

---

## 上线检查清单

按顺序打勾，全绿就可以发网址给朋友了：

- [ ] 仓库是 **Public**
- [ ] Token 只勾了 `xiuxian-game` 这一个仓库，权限是 Contents 读写 + Actions 读写
- [ ] `js/api.js` 里 owner / repo / token 三项都改成了你自己的
- [ ] 仓库根目录能看到 `index.html`（不是在某个子文件夹里）
- [ ] Settings → Actions → Workflow permissions 是 **Read and write**
- [ ] Actions 手动跑过一次是 **绿色 ✔**
- [ ] Settings → Pages 显示 **Your site is live at ...**
- [ ] 游戏网址能打开登录页
- [ ] 注册角色后，仓库 `data/players/` 下**生成了 .json 文件**
- [ ] 打一场战斗，动画正常
- [ ] 底部状态是**绿点**
- [ ] 后台能看到玩家列表

---

## 需要帮助时

把下面这些信息发给我，我能直接定位问题：
1. 你的仓库网址
2. 截图：游戏「⚙️ 设置」页面
3. 截图：Actions 页面的运行记录
4. 浏览器按 F12 → Console 里的红色报错文字
