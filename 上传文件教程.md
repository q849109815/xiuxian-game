# 逐个上传文件教程（含创建文件夹）

> 专门给「不会用命令行」的你。**全程在网页上点鼠标**，不需要装任何软件。
> 按下面的顺序走一遍，大约 15 分钟搞定。

---

## 📌 先看这个：GitHub 建文件夹的唯一技巧

GitHub 网页上**没有「新建文件夹」按钮**。唯一的办法是：

> **新建文件时，在文件名里打一个斜杠 `/`，GitHub 就会自动帮你建出这个文件夹。**

比如文件名框里填 `js/.gitkeep`，提交后仓库里就自动生成了一个 `js` 文件夹。
再比如填 `admin/js/.gitkeep`，一次就能生成**两级**文件夹 `admin/js`。

`.gitkeep` 只是个空占位文件（名字随便，习惯用这个），目的纯粹是为了"把文件夹撑起来"。
文件夹建好之后，就可以进去上传真正的游戏文件了。

---

## 📋 总览：你要传的东西

**9 个文件夹 + 18 个文件**（`README.md` 建仓库时已经有了，不用传）

| 顺序 | 文件夹 | 里面要放的文件 |
|---|---|---|
| ① | 根目录（不用建） | `index.html`、`setup.html`、`部署教程.md` |
| ② | `css` | `style.css` |
| ③ | `js` | `api.js`、`engine.js`、`ui.js`、`main.js` |
| ④ | `admin` | `index.html` |
| ⑤ | `admin/js` | `admin.js` |
| ⑥ | `data/config` | `game.json`、`notice.json` |
| ⑦ | `data/players` | （空文件夹，只放占位文件） |
| ⑧ | `scripts` | `aggregate.mjs`、`balance_sim.cjs` |
| ⑨ | `worker/src` | `index.js` |
| ⑩ | `worker` | `wrangler.toml` |
| ⑪ | `.github/workflows` | `aggregate.yml` |
| ⑫ | `backups` | （空文件夹，只放占位文件） |

> 💡 **同一个文件夹里的文件可以一次拖完**，不用真的一个一个拖。真正要分开做的是"文件夹"。

---

## 第一步：建好全部 9 个文件夹

下面这 9 个操作**动作完全一样**，只是填的名字不同。每个大约 10 秒。

### 通用动作（记下来，下面重复 9 次）

1. 回到仓库主页 `github.com/你的用户名/xiuxian-game`
2. 点文件列表右上方的绿色 **Add file** ▼ 按钮
3. 菜单里点 **Create new file**
4. 上方**文件名框**里输入表格里的名字
5. 下面**内容区什么都不用填**，保持空白
6. 点绿色 **Commit changes**（弹出框再点一次 **Commit changes**）

### 依次填这 9 个名字

| # | 文件名框里填这个 | 会生成什么 |
|---|---|---|
| 1 | `css/.gitkeep` | css 文件夹 |
| 2 | `js/.gitkeep` | js 文件夹 |
| 3 | `admin/js/.gitkeep` | admin 和 admin/js 两级 |
| 4 | `data/config/.gitkeep` | data 和 data/config 两级 |
| 5 | `data/players/.gitkeep` | data/players（放玩家存档的，**必须建**） |
| 6 | `scripts/.gitkeep` | scripts 文件夹 |
| 7 | `worker/src/.gitkeep` | worker 和 worker/src 两级 |
| 8 | `.github/workflows/.gitkeep` | .github 和 .github/workflows 两级（开头有个点） |
| 9 | `backups/.gitkeep` | backups 文件夹 |

✅ **检查点**：仓库主页现在应该能看到这些文件夹：`css`、`js`、`admin`、`data`、`scripts`、`worker`、`.github`、`backups`。
点进 `js` 能看到一个 `.gitkeep`；点进 `admin` 能看到 `js`；点进 `data` 能看到 `config` 和 `players`。

> ⚠️ 如果 `.github` 或 `backups` 看不见：别慌，GitHub 会把**空的、以点开头的**文件夹排在最后面，往下翻找找。实在找不到就重新做一次第 8 项。

---

## 第二步：上传根目录文件（3 个）

1. 仓库主页 → **Add file** ▼ → **Upload files**
2. 打开电脑上解压出来的 `xiuxian` 文件夹
3. 把这三个文件**一起拖**进虚线框：
   - `index.html`
   - `setup.html`
   - `部署教程.md`
4. 等缩略图出来 → 点绿色 **Commit changes**

> `README.md` 建仓库时已经生成了一个，**不要拖**，否则会冲突提示。

✅ 检查点：主页能看到 `index.html`、`setup.html`、`部署教程.md`。

---

## 第三步：上传 `css`（1 个）

1. 主页点 **`css`** 文件夹进去
2. 点 **Add file** ▼ → **Upload files**
3. 拖入本地 `xiuxian/css/` 里的 **`style.css`**
4. **Commit changes**

---

## 第四步：上传 `js`（4 个 · 最重要）

1. 点左上角 **`xiuxian-game`** 面包屑回主页 → 点 **`js`** 进去
2. **Add file** ▼ → **Upload files**
3. 一次性拖入本地 `xiuxian/js/` 里的 4 个文件：
   - `api.js` ← **第 4 步配置要改的就是它**
   - `engine.js`
   - `ui.js`
   - `main.js`
4. **Commit changes**

✅ 检查点：点进 `js` 文件夹，能看到 **4 个 .js 文件**，一个都不能少。少了游戏就是白屏。

---

## 第五步：上传 `admin`（1 个）

1. 回主页 → 点 **`admin`** 进去
2. **Add file** ▼ → **Upload files**
3. 拖入本地 `xiuxian/admin/` 里的 **`index.html`**
4. **Commit changes**

---

## 第六步：上传 `admin/js`（1 个）

1. 现在你在 `admin` 文件夹里，点里面的 **`js`** 进去
2. **Add file** ▼ → **Upload files**
3. 拖入本地 `xiuxian/admin/js/` 里的 **`admin.js`**
4. **Commit changes**

---

## 第七步：上传 `data/config`（2 个）

1. 回主页 → 点 **`data`** → 点 **`config`**
2. **Add file** ▼ → **Upload files**
3. 拖入本地 `xiuxian/data/config/` 里的：
   - `game.json` ← 游戏全部数值都在这
   - `notice.json`
4. **Commit changes**

> `data/players` 不用传东西，第一步建的 `.gitkeep` 已经把它撑起来了。

---

## 第八步：上传 `scripts`（2 个）

1. 回主页 → 点 **`scripts`**
2. **Upload files** → 拖入：
   - `aggregate.mjs`
   - `balance_sim.cjs`
3. **Commit changes**

---

## 第九步：上传 `worker`（2 个，分两个文件夹）

**9-1. `worker/src`：**
1. 回主页 → 点 **`worker`** → 点 **`src`**
2. **Upload files** → 拖入 **`index.js`**
3. **Commit changes**

**9-2. `worker` 根目录：**
1. 点面包屑回到 **`worker`** 这一层（不是 src 里面）
2. **Upload files** → 拖入 **`wrangler.toml`**
3. **Commit changes**

---

## 第十步：上传 `.github/workflows`（1 个 · 隐藏文件夹）

1. 回主页，往下翻找到 **`.github`**（开头带点，排在靠后），点进去
2. 点 **`workflows`** 进去
3. **Upload files** → 拖入本地 `xiuxian/.github/workflows/` 里的 **`aggregate.yml`**
4. **Commit changes**

> 💡 **Windows 上找不到 `.github` 文件夹？** 打开资源管理器 → 顶部「查看」→ 勾选「**隐藏的项目**」，就能看到了。
>
> 💡 **实在找不到就用「粘贴法」代替**，效果完全一样：
> 主页 **Add file** ▼ → **Create new file** → 文件名框填 `.github/workflows/aggregate.yml`
> → 用记事本打开本地那个文件，全选复制，粘贴进内容区 → Commit changes。

✅ 检查点：`.github/workflows/` 里有 `aggregate.yml`。**这个文件漏了排行榜就不会更新。**

---

## 🎯 上传完成后核对

回到仓库主页，点一遍，应该**完全符合**这棵树：

```
xiuxian-game/
├── index.html          ← 游戏主页
├── setup.html          ← 配置向导
├── 部署教程.md
├── README.md           （建仓库时自带的）
├── .github/
│   └── workflows/
│       └── aggregate.yml
├── admin/
│   ├── index.html
│   └── js/
│       └── admin.js
├── backups/
│   └── .gitkeep
├── css/
│   └── style.css
├── data/
│   ├── config/
│   │   ├── game.json
│   │   └── notice.json
│   └── players/
│       └── .gitkeep
├── js/
│   ├── api.js          ★ 下一步要改这个
│   ├── engine.js
│   ├── ui.js
│   └── main.js
├── scripts/
│   ├── aggregate.mjs
│   └── balance_sim.cjs
└── worker/
    ├── wrangler.toml
    └── src/
        └── index.js
```

**只要这棵树对上了，上传就 100% 成功了。**

---

## ⚠️ 上传时最容易犯的 4 个错

| 错误现象 | 后果 | 正确做法 |
|---|---|---|
| **拖文件前忘了先进文件夹** | 所有文件都堆在根目录，游戏白屏 | 每次上传前，先点进目标文件夹，再点 Add file |
| **拖整个 `xiuxian` 文件夹进去** | 变成 `xiuxian/xiuxian/index.html`，网址打不开 | 只拖文件夹**里面**的文件，不拖外层文件夹 |
| **`js` 里只传了 2、3 个文件** | 游戏打不开或按钮没反应 | 必须 4 个齐全：api / engine / ui / main |
| **`.github/workflows/aggregate.yml` 漏了** | 游戏能玩，但排行榜永远是空的 | 第十步一定要做 |

**判断自己有没有犯错的万能方法**：看仓库主页有没有 `index.html`。
如果主页只有一堆 `.js` 和 `.css` 散文件，说明第 2 条犯了，删掉重传。

---

## 🚀 更省事的三种替代方案（可选）

如果你觉得上面太繁琐，这三个方法**一次就能传完所有文件夹**：

### 方案 A：按键盘句号键（推荐试试）
仓库主页，直接按键盘上的 **`.`**（句号）。会打开一个**网页版 VS Code 编辑器**。
左边有文件树，可以直接把本地 `xiuxian` 文件夹**整个拖进去**，文件夹结构会原样保留。
传完点左边源代码管理图标 → 输入说明 → 点勾提交。

### 方案 B：GitHub Desktop（图形软件，最适合新手）
1. 下载 https://desktop.github.com 并安装，登录
2. **File → Add local repository** → 选本地 `xiuxian` 文件夹
3. 左下角填 Summary（随便写 `init`）→ 点 **Commit to main**
4. 点顶部 **Publish repository** → 取消勾选 "Keep this code private" → **Publish**
5. 一次性全传完，文件夹结构完全正确

### 方案 C：命令行（3 行）
```bash
cd 本地 xiuxian 文件夹的路径
git init && git add . && git commit -m "init"
git remote add origin https://github.com/你的用户名/xiuxian-game.git && git push -u origin main
```
密码填部署教程第 2 步生成的 Token。

---

## 上传完之后

回到 **`部署教程.md`**，从 **第 4 步：填写配置** 继续往下做。

> 第 4 步推荐用 `setup.html` 配置向导：双击本地的 `setup.html`，填用户名和 Token，
> 点「生成配置」→「复制配置」，然后回到仓库打开 `js/api.js`，
> 把开头那整段 `const GH = { ... };` 删掉，粘贴新的，Commit 即可。
