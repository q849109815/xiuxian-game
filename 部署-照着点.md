# 部署：照着点（基于实测，不再猜界面）

## 先说清楚三件事

1. **Cloudflare 我没法替你点** —— 那是你的账号，需要你本人登录。我进不去，任何说"我帮你弄"的都是骗你的。
2. **教程之前有问题，是我的错** —— 我按老版界面写，你按新版点，对不上。现在这版是按你**实际截图**写的。
3. **你其实已经做对一大半了** —— 授权页面你选了 "Only select repositories" + 只勾 `xiuxian-game`，这是最正确的做法。

---

## 你现在在哪一步

你最后一张截图是 **Install & Authorize**（安装并授权）页面。

如果你点了那个绿色按钮 → 现在应该在 **Pages 配置页**。
如果没点 → 回到 Workers & Pages 页，点 Create，重新走一次授权。

---

## 配置页怎么填（如果你现在在配置页）

页面上会有几个输入框，按这个填：

| 框的名字 | 填什么 | 不填会怎样 |
|---|---|---|
| **Project name** / 项目名 | `xiuxian-game` | 网址会变，不影响使用 |
| **Production branch** / 生产分支 | `main` | 下拉框选 `main` |
| **Framework preset** / 框架预设 | **`None`** | ⚠️ 选了 Vite/React 必失败 |
| **Build command** / 构建命令 | `exit 0` | ⚠️ 就这两个字，中间有空格 |
| **Build output directory** / 输出目录 | `/` | ⚠️ 就一个斜杠，填错必 404 |

填完点 **Save and Deploy**（保存并部署），等 1～2 分钟。

拿到网址长这样：

```
https://xiuxian-game.pages.dev
```

**这步做完，游戏就能打开了。先试试，能进就成功了。**

---

## 如果你卡在别的地方

**把当前页面截图发我，我按图告诉你点哪。** 这次不猜了。

---

## 我已经帮你做完的部分（不用你管）

这几件事我在 GitHub 侧已经做完了，你不需要操作：

### ✅ 1. 修好了"离线"的真凶

之前后台一直显示离线，我以为是端点不通。**实测发现真正原因是超时太短：**

```
官方端点实际响应时间：5.47 秒
我原来设的超时：     3.5 秒   ← 每次都在成功前放弃了
```

已经把超时改成 10～15 秒。**现在后台和游戏应该能连上了，你可以先去刷新试试。**

### ✅ 2. 存档挪到独立分支

新建了 `players` 分支，存档写入它。已验证隔离有效：

```
players 分支：有存档文件 ✔
main 分支：   无存档文件 ✔（正确隔离）
```

**好处**：你连了 Git 之后，Cloudflare Pages 监听 `main`。存档提交到 `players` 分支，**不会触发重建**，每月 500 次构建额度就不会被烧光了。

这个坑我提前帮你填了，你不用管。

### ✅ 3. 游戏本身的修复

- 战斗掉落吸附范围扩大
- 邮件系统接入
- 场景背景图、角色立绘接入
- 后台 9 个页面全部可用

---

## 关于加速代理（Worker）

**这一步可以晚点做。** 先确认游戏能打开、能玩，再说。

它的作用是解决"存档存不了"。如果你发现：
- 游戏底部是**红点「○ 离线」**
- 打完一局，GitHub 仓库 `data/zb/players/` 里**没有**你的 `.json` 文件

那就需要做 Worker。到时候把截图发我，我带着你做。

---

## 一句话总结

**你现在要做的只有一件事**：在 Cloudflare 配置页填 5 个框 → Save and Deploy → 拿到 `pages.dev` 网址。

其余的（超时修复、分支隔离、游戏 bug）我已经全部做完了。

卡住就截图，我按图说。
